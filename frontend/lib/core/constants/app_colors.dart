import 'package:flutter/material.dart';

class AppColors {
  // Brand Palette
  static const Color primary = Color(0xFF1E3A8A); // Deep Navy
  static const Color primaryLight = Color(0xFF3B82F6); // Electric Blue
  static const Color primaryDark = Color(0xFF0F172A); // Midnight Slate

  // AI & Automation Accent
  static const Color aiPurple = Color(0xFF7C3AED);
  static const Color aiPurpleLight = Color(0xFFF5F3FF);
  static const Color aiPurpleBorder = Color(0xFFDDD6FE);

  // Human Verified Accent
  static const Color verifiedGreen = Color(0xFF059669);
  static const Color verifiedGreenLight = Color(0xFFECFDF5);

  // Priorities & Severities
  static const Color priorityCritical = Color(0xFFDC2626); // P1 / Critical
  static const Color priorityHigh = Color(0xFFEA580C);     // P2 / High
  static const Color priorityMedium = Color(0xFFD97706);   // P3 / Medium
  static const Color priorityLow = Color(0xFF2563EB);      // P4 / Low

  // Neutral Tones
  static const Color background = Color(0xFFF8FAFC);
  static const Color cardSurface = Colors.white;
  static const Color border = Color(0xFFE2E8F0);
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF475569);
  static const Color textMuted = Color(0xFF94A3B8);

  // Role Accents
  static const Color roleStudent = Color(0xFF2563EB);
  static const Color roleOperator = Color(0xFF4F46E5);
  static const Color roleTechnician = Color(0xFF0D9488);
  static const Color roleAdmin = Color(0xFF9333EA);
}
